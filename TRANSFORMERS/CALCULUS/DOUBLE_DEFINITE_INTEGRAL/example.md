In this example, `DOUBLE_DEFINITE_INTEGRAL` node generates a Scalar type value.

The value is added with the `SINE` node which generates a larger sine function which is visable through `SCATTER` node.