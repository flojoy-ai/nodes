In this example, the `CPU_RAM` node returns the RAM available on the system running Flojoy.


The 'Big Number' node displays the scalar returned by the 'CPU_RAM' node, it represents the RAM available in Go.

