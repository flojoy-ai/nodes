COMMAND:
  - {
      name: Camera,
      key: CAMERA,
      type: WEB_CAM,
      parameters:
        {
          file_type: { type: "string", default: "image" },
          camera_ind: { type: "integer", default: 0 },
        }
    }
