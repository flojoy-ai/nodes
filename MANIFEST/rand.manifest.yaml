COMMAND:
  - name: Random (Uniform)
    key: RAND
    type: SIMULATION
    parameters:
      distribution:
        type: select
        options:
          - normal
          - uniform
          - poisson
          - dirichlet
        default: uniform
