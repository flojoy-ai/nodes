In this example we're simulating data from `LINSPACE`, `TIMESERIES`, `MATRIX` and `R_DATASET` and visualizing them with `HISTOGRAM` node which creates a Plotly Histogram visualization for each of the input node.